<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <title>{{.IndexData.TUMLiveContext.Course.Name}} | TUM Live</title>
    {{template "headImports" .IndexData.VersionTag}}
</head>
<body>
{{- /*gotype: TUM-Live/web.CoursePageData*/ -}}
{{template "header" .IndexData.TUMLiveContext}}
{{$course := .IndexData.TUMLiveContext.Course}}
<!-- 5.75 = nav height + padding -->
<div class="container" style="height: calc(100vh - (5.75rem));">
    <div class="mb-8 mt-3">
        <h1 class="font-bold text-3xl dark:text-white my-auto">{{$course.Name}}</h1>
    </div>
    <div class="m-auto flex flex-wrap pb-3 md:h-5/6 md:grid md:grid-rows-6 md:grid-cols-2 md:gap-4 md:pb-0">
        <!-- order.false = DESC, order.true = ASC -->
        <div x-data="{asc: false}"
             class="bg-white h-full w-full border dark:bg-secondary-lighter dark:border-gray-900 rounded-lg
                    shadow-sm overflow-y-scroll md:row-span-6 md:w-auto">

            <div class="flex justify-between sticky top-0 bg-white dark:bg-secondary-lighter py-4 px-3 border-b
                dark:border-gray-900 mb-3">
                <span class="bg-success rounded-full text-lg font-bold text-white px-5">VoD</span>
                <button class="hover:bg-gray-200 dark:hover:bg-gray-900 rounded px-2 my-auto"
                        @click="asc = !asc;">
                    <span class="text-sm font-semibold uppercase dark:text-white"
                          x-text="asc ? '&#x25B2; asc' : '&#x25BC; desc'">&#x25BC; desc</span>
                </button>
            </div>

            <ul class="flex px-5 pb-3" :class="asc ? 'flex-col-reverse' : 'flex-col'">
                {{template "vod_course_list" .IndexData.TUMLiveContext}}
            </ul>
        </div>
        <div class="flex w-full relative bg-white border dark:bg-secondary-lighter dark:border-gray-900 rounded-lg
                    shadow-sm py-3 px-5 my-3 order-first md:my-0 md:row-span-1 md:w-auto md:order-none">
            {{$lecture := $course.GetNextLecture}}
            {{if $lecture.LiveNow}}
                <span class="absolute -top-2 -right-2 flex h-5 w-5">
                  <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-700 opacity-75"></span>
                  <span class="relative inline-flex rounded-full h-5 w-5 bg-danger"></span>
                </span>
                <div class="my-auto">
                    <a href="/w/{{$course.Slug}}/{{$lecture.Model.ID}}">
                        <h1 class="text-3xl font-bold dark:text-white"><i class="fas fa-angle-right"></i> Live Now!</h1>
                    </a>
                </div>
            {{else if $lecture.IsComingUp}}
                <span class="absolute -top-2 -right-2 flex h-5 w-5">
                  <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-amber-500 opacity-75"></span>
                  <span class="relative inline-flex rounded-full h-5 w-5 bg-wait"></span>
                </span>
                <div class="my-auto">
                    <h1 class="text-3xl font-bold dark:text-white">Live Soon!</h1>
                    <a class="font-light text-sm dark:text-white" href="/w/{{$course.Slug}}/{{$lecture.Model.ID}}">
                        <i class="fas fa-angle-right"></i>
                        <span>Join waiting room</span>
                    </a>
                </div>
            {{else}}
                <div class="my-auto">
                    <h1 class="text-3xl font-bold dark:text-white">
                        {{$next := $lecture.Start}} {{printf "%v %02d. %02d:%02d" $next.Month $next.Day $next.Hour $next.Minute}}
                    </h1>
                    <span class="font-light text-sm dark:text-white">Next Livestream</span>
                </div>
            {{end}}
        </div>
        <div class="md:row-span-5 w-full md:w-auto">
            <div x-data="{showPlanned: false}"
                 class="bg-white border dark:bg-secondary-lighter dark:border-gray-900 rounded-lg shadow-sm
                 overflow-y-scroll my-3 max-h-full md:my-0">

                <div class="flex justify-between py-4 px-3 sticky top-0 bg-white dark:bg-secondary-lighter"
                     :class="showPlanned && 'border-b dark:border-gray-900 mb-3'">
                    <span class="bg-info rounded-full text-lg font-bold text-white px-5">Upcoming</span>
                    <button class="hover:bg-gray-200 dark:hover:bg-gray-600 rounded px-2 my-auto"
                            @click="showPlanned = !showPlanned;">
                        <span class="text-sm font-semibold uppercase dark:text-white"
                              x-text="showPlanned ? '&#x25B2; hide' : '&#x25BC; show'">&#x25BC; show</span>
                    </button>
                </div>

                <ul x-show="showPlanned" class="px-5 pb-3">
                    {{template "planned_course_list" .IndexData.TUMLiveContext}}
                </ul>
            </div>
        </div>
    </div>
</div>
</body>
</html>
