<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>TUM Live</title>
    {{template "headImports"}}
</head>
<body class="bg-primary">
{{- /*gotype: TUM-Live/web.IndexData*/ -}}
{{template "header" .}}

<div class="container pt-6">
    <h2 class="text-2xl text-white text-center">{{if not .LiveStreams}}No Active Livestreams{{else}}Currently Live:{{end}}</h2>
    {{range $liveStream := .LiveStreams }}
        <div>
            <a href="/live/{{$liveStream.Model.ID}}"><h3 class="text-xl text-gray-300">{{$liveStream.Name}}</h3></a>
        </div>
    {{end}}
</div>
<div class="container pt-6">
    <h2 class="text-2xl text-white text-center">{{if not .Courses}}You are not enrolled in any Courses that use TUM Live.{{else}}My Courses:{{end}}</h2>
    {{range $course := .Courses }}
        <div>
            <a href="/c/{{$course.TeachingTerm}}/{{$course.Slug}}"><h3 class="text-xl text-gray-300">{{$course.Name}}</h3></a>
        </div>
    {{end}}
</div>
</body>
</html>
